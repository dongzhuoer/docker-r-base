language: generic
services: docker
addons:
  apt:
    packages:
      - docker-ce

script:
  - docker build -t dongzhuoer/rlang:base base
after_success:
  - echo "$DOCKER_PASSWORD" | docker login -u dongzhuoer --password-stdin
  - docker push dongzhuoer/rlang:base
